浏览器驱动：本机 Edge 独立调试 profile `C:/Users/18601/.hermes/edge-debug` + CDP 9222，browser_exec session 固定 "cw"。Edge 必须用 terminal background=true 启动（前台命令超时会连 Edge 一起杀）。用户正常 Edge 无调试端口、系统默认浏览器是360(SLBrowserHTML)，均无法/不应接管。会话报 bu-cw.port PermissionError 时先 `rm -f ~/.config/browser-harness/runtime/bu-cw.*` 再重试。财务云审批规则与 DOM 锚点全在 skill `ccccltd-approval`（含批量脚本 scripts/approve.py）。对话关闭后需重启调试 Edge 再走门户 SSO 入口；登录态持久化在调试 profile。
